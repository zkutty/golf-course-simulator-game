# Copy to .env.local only if a local wrapper explicitly loads it.
# Prefer exporting this value in the parent shell so Codex/MCP inherits it.
# Never commit a real value. Rotate any credential pasted into chat or logs.
PIXELLAB_AUTH_HEADER="Bearer replace-with-a-rotated-pixellab-token"
