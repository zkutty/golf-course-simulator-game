# Local legacy audio quarantine

This ignored directory preserves local copies of older MP3 files that were
previously stored directly under `public/audio/`. Their provenance is not
confirmed, they are not referenced by the application, and they must never be
copied into a build or deployment artifact.
